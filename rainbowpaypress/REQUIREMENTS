RainbowPayPress requires:


    Client-side environment:

     * Generic web browser with HTML5 support.
     * JavaScript enabled in the web browser.


    Server-side environment:

     * PHP version >= 5.4
     * php5-curl module enabled
     * WordPress version >= 3.8.3
